{
  "name": "svg-sanitizer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "rm -rf dist/* && node index.js",
    "test": "cd dist && svg-sprite --symbol --symbol-inline --symbol-render-scss --symbol-example --dest=out './*.svg'"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "svg-sprite": "^1.5.0",
    "svgo": "^1.3.2"
  }
}
